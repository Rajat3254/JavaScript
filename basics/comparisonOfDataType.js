console.log(null>0);
console.log(null>=0);
console.log(null==0);
// comparision convert null to 0 whereas equality doesn't so the output is false true false
//but in case of undefined the comparision also doesn't able to do so


